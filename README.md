# C Paint

A simple paint program written in C using SDL2.

## Requirements

Install SDL2:

```bash
sudo apt update
sudo apt-get install libsdl2-2.0-0
```

Compile:

```bash
gcc main.c -o paint `sdl2-config --cflags --libs` -lm
```


